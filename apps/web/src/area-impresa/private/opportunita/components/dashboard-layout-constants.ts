/**
 * Shared between the requests-dashboard shell (empty-state rail) and the
 * intercepted detail panel route, so both dock at the exact same vertical
 * offset — right below the (fixed) topbar + KPI strip stack.
 */
export const PANEL_TOP_OFFSET = "174px"
