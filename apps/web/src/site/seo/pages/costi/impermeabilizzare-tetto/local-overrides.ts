import type { CityLocalOverride } from "../types";

/**
 * Nessuna pagina città per questa guida (stesso pattern di rifare-tetto):
 * nessun local override reale oggi. Il composer gestisce un array vuoto
 * producendo cityPages/citySections vuoti.
 */
export const impermeabilizzareTettoLocalOverrides: readonly CityLocalOverride[] = [];
